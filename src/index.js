
import { h, app } from 'hyperapp'
import './index.css'

const state = {
}

const actions = {
}

const view = (s, a) => (
  <div>
    <h1>
      Have a great day bb!
    </h1>
  </div>
)

app(state, actions, view, document.body)
